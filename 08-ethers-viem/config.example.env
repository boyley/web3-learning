# 复制为 .env 后使用（.env 已被 .gitignore 忽略，绝不提交）
# 教学 demo 中所有【只读】操作都不需要任何密钥，直接用公共 RPC 即可。
# 仅当你想用 Node 脚本【发交易】时（本教学不推荐，浏览器请走 MetaMask）才需要私钥。
# 绝不要在这里填主网私钥！只用一个专门的测试网小号私钥。
TEST_PRIVATE_KEY=

# 可选：自定义 RPC（留空则用代码里的公共默认 RPC）
SEPOLIA_RPC_URL=
MAINNET_RPC_URL=
