require("@nomicfoundation/hardhat-toolbox");

/** @type import('hardhat/config').HardhatUserConfig */
module.exports = {
  solidity: "0.8.28",
  // 本模块演示部署到内置 hardhat 链 / 本地 localhost 节点。
  // 连测试网的写法见 07-verify-etherscan。
};
